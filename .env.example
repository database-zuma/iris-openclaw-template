# Credentials Template — Copy this to .env and fill in your actual values
# DO NOT commit .env to version control!

# GitHub (for code commits, repo management)
GITHUB_TOKEN=ghp_your_github_token_here
GITHUB_USER=your_github_username

# Vercel (for deployments, if needed)
VERCEL_TOKEN=your_vercel_token_here

# PostgreSQL Database (if you have one)
PGHOST=your_postgres_host
PGPORT=5432
PGDATABASE=your_database_name
PGUSER=your_database_user
PGPASSWORD=your_database_password
DATABASE_URL=postgresql://user:password@host:5432/dbname

# macOS Admin Password (for elevated operations, optional)
MACOS_ADMIN_PASSWORD=your_macos_password

# Notion API (if using Notion integration)
NOTION_API_KEY=ntn_your_notion_api_key_here

# Google Workspace (if using gog CLI)
GOOGLE_ACCOUNT=your_email@gmail.com
GOG_BIN=/path/to/gog

# Admin Contact (WhatsApp number for alerts)
WAYAN_WA=+1234567890
